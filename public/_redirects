# Static assets should be served directly
/templates/*  /templates/:splat  200
/static/*     /static/:splat     200
/styles.*     /styles.*     200
/main.*       /main.:splat       200

# SPA fallback for all other routes
/*  /index.html  200

# Only redirect the default Netlify subdomain, don't interfere with custom domains
# Remove this line if both .com and .ru domains are on the same Netlify site
# https://smddev.netlify.com/* https://smddev.com/:splat 301!
